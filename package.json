{
  "name": "fireats",
  "version": "0.0.0",
  "description": "Starting point for the Cloud Firestore codelab",
  "main": ".",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fmt": "prettier --write scripts/* styles/*"
  },
  "author": "",
  "devDependencies": {
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^0.6.0",
    "firebase": "^4.5.2",
    "firebase-admin": "^4.1.1",
    "firebase-functions": "^0.7.2"
  }
}
